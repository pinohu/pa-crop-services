{
  "nav_html": "    <div class=\"nav-item\" onclick=\"setTab('compliance_center', this)\"><span class=\"nav-icon\">\ud83d\udee1\ufe0f</span>Compliance Center</div>\n    <div class=\"nav-item\" onclick=\"setTab('filing_actions', this)\"><span class=\"nav-icon\">\ud83d\udcdd</span>Filing Actions</div>\n    <div class=\"nav-item\" onclick=\"setTab('entity_mgmt', this)\"><span class=\"nav-icon\">\ud83c\udfdb\ufe0f</span>My Entities</div>\n    <div class=\"nav-item\" onclick=\"setTab('ai_assistant', this)\"><span class=\"nav-icon\">\ud83e\udd16</span>AI Assistant</div>\n    <div class=\"nav-item\" onclick=\"setTab('billing_center', this)\"><span class=\"nav-icon\">\ud83d\udcb3</span>Billing</div>\n    <div class=\"nav-item\" onclick=\"setTab('partner_referral', this)\"><span class=\"nav-icon\">\ud83c\udfaf</span>Referrals</div>",
  "tabs_html": "\n    <div class=\"tab-content\" id=\"tab-compliance_center\" style=\"display:none\">\n      <h2 style=\"font-family:var(--serif);margin-bottom:16px\">Compliance Center</h2>\n      <div id=\"compliance_center-body\"><div style=\"text-align:center;padding:32px;color:var(--muted)\">Loading...</div></div>\n    </div>\n\n    <div class=\"tab-content\" id=\"tab-filing_actions\" style=\"display:none\">\n      <h2 style=\"font-family:var(--serif);margin-bottom:16px\">Filing Actions</h2>\n      <div id=\"filing_actions-body\"><div style=\"text-align:center;padding:32px;color:var(--muted)\">Loading...</div></div>\n    </div>\n\n    <div class=\"tab-content\" id=\"tab-entity_mgmt\" style=\"display:none\">\n      <h2 style=\"font-family:var(--serif);margin-bottom:16px\">My Entities</h2>\n      <div id=\"entity_mgmt-body\"><div style=\"text-align:center;padding:32px;color:var(--muted)\">Loading...</div></div>\n    </div>\n\n    <div class=\"tab-content\" id=\"tab-ai_assistant\" style=\"display:none\">\n      <h2 style=\"font-family:var(--serif);margin-bottom:16px\">AI Assistant</h2>\n      <div id=\"ai_assistant-body\"><div style=\"text-align:center;padding:32px;color:var(--muted)\">Loading...</div></div>\n    </div>\n\n    <div class=\"tab-content\" id=\"tab-billing_center\" style=\"display:none\">\n      <h2 style=\"font-family:var(--serif);margin-bottom:16px\">Billing</h2>\n      <div id=\"billing_center-body\"><div style=\"text-align:center;padding:32px;color:var(--muted)\">Loading...</div></div>\n    </div>\n\n    <div class=\"tab-content\" id=\"tab-partner_referral\" style=\"display:none\">\n      <h2 style=\"font-family:var(--serif);margin-bottom:16px\">Referrals</h2>\n      <div id=\"partner_referral-body\"><div style=\"text-align:center;padding:32px;color:var(--muted)\">Loading...</div></div>\n    </div>",
  "loaders_js": "\nasync function loadPortal_compliance_center() {\n  if (!CLIENT) return;\n  const el = document.getElementById('compliance_center-body');\n  const endpoints = [\n    { route: '/api/compliance-score', label: 'Your compliance score' }\n    { route: '/api/compliance-dashboard', label: 'Compliance overview' }\n    { route: '/api/dissolution-predictor', label: 'Dissolution risk check' }\n    { route: '/api/entity-monitor', label: 'Entity monitoring' }\n    { route: '/api/compliance-check', label: 'Quick compliance check' }\n  ];\n  let html = '<div style=\"display:grid;gap:12px\">';\n  for (const ep of endpoints) {\n    try {\n      const url = ep.route + (ep.route.includes('?') ? '&' : '?') + 'email=' + encodeURIComponent(CLIENT.email);\n      const r = await fetch(url);\n      const ct = r.headers.get('content-type') || '';\n      if (!ct.includes('json')) { html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid var(--border)\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:var(--muted)\">Connected \u00b7 ${ep.route}</div></div>`; continue; }\n      const d = await r.json();\n      const ok = d.success !== false && r.ok;\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid ${ok?'var(--border)':'#fca5a5'}\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:${ok?'var(--muted)':'#dc2626'}\">${ok?'Available':'Service unavailable'}</div>${ok && d.score !== undefined ? '<div style=\"font-size:24px;font-weight:700;margin-top:8px\">'+d.score+'</div>' : ''}</div>`;\n    } catch(e) {\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid #fca5a5\"><div style=\"font-weight:600;font-size:14px\">${ep.label}</div><div style=\"font-size:12px;color:#dc2626\">${e.message}</div></div>`;\n    }\n  }\n  html += '</div>';\n  el.innerHTML = html;\n}\n\nasync function loadPortal_filing_actions() {\n  if (!CLIENT) return;\n  const el = document.getElementById('filing_actions-body');\n  const endpoints = [\n    { route: '/api/annual-report-prefill', label: 'Pre-fill annual report' }\n  ];\n  let html = '<div style=\"display:grid;gap:12px\">';\n  for (const ep of endpoints) {\n    try {\n      const url = ep.route + (ep.route.includes('?') ? '&' : '?') + 'email=' + encodeURIComponent(CLIENT.email);\n      const r = await fetch(url);\n      const ct = r.headers.get('content-type') || '';\n      if (!ct.includes('json')) { html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid var(--border)\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:var(--muted)\">Connected \u00b7 ${ep.route}</div></div>`; continue; }\n      const d = await r.json();\n      const ok = d.success !== false && r.ok;\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid ${ok?'var(--border)':'#fca5a5'}\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:${ok?'var(--muted)':'#dc2626'}\">${ok?'Available':'Service unavailable'}</div>${ok && d.score !== undefined ? '<div style=\"font-size:24px;font-weight:700;margin-top:8px\">'+d.score+'</div>' : ''}</div>`;\n    } catch(e) {\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid #fca5a5\"><div style=\"font-weight:600;font-size:14px\">${ep.label}</div><div style=\"font-size:12px;color:#dc2626\">${e.message}</div></div>`;\n    }\n  }\n  html += '</div>';\n  el.innerHTML = html;\n}\n\nasync function loadPortal_entity_mgmt() {\n  if (!CLIENT) return;\n  const el = document.getElementById('entity_mgmt-body');\n  const endpoints = [\n    { route: '/api/entity-intake', label: 'Add new entity' }\n    { route: '/api/entity-database', label: 'Entity registry' }\n    { route: '/api/pa-dos-search', label: 'PA DOS lookup' }\n    { route: '/api/address-verify', label: 'Verify address' }\n  ];\n  let html = '<div style=\"display:grid;gap:12px\">';\n  for (const ep of endpoints) {\n    try {\n      const url = ep.route + (ep.route.includes('?') ? '&' : '?') + 'email=' + encodeURIComponent(CLIENT.email);\n      const r = await fetch(url);\n      const ct = r.headers.get('content-type') || '';\n      if (!ct.includes('json')) { html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid var(--border)\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:var(--muted)\">Connected \u00b7 ${ep.route}</div></div>`; continue; }\n      const d = await r.json();\n      const ok = d.success !== false && r.ok;\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid ${ok?'var(--border)':'#fca5a5'}\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:${ok?'var(--muted)':'#dc2626'}\">${ok?'Available':'Service unavailable'}</div>${ok && d.score !== undefined ? '<div style=\"font-size:24px;font-weight:700;margin-top:8px\">'+d.score+'</div>' : ''}</div>`;\n    } catch(e) {\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid #fca5a5\"><div style=\"font-weight:600;font-size:14px\">${ep.label}</div><div style=\"font-size:12px;color:#dc2626\">${e.message}</div></div>`;\n    }\n  }\n  html += '</div>';\n  el.innerHTML = html;\n}\n\nasync function loadPortal_ai_assistant() {\n  if (!CLIENT) return;\n  const el = document.getElementById('ai_assistant-body');\n  const endpoints = [\n    { route: '/api/assistant/summarize-document', label: 'Summarize documents' }\n    { route: '/api/assistant/explain-obligation', label: 'Explain obligations' }\n  ];\n  let html = '<div style=\"display:grid;gap:12px\">';\n  for (const ep of endpoints) {\n    try {\n      const url = ep.route + (ep.route.includes('?') ? '&' : '?') + 'email=' + encodeURIComponent(CLIENT.email);\n      const r = await fetch(url);\n      const ct = r.headers.get('content-type') || '';\n      if (!ct.includes('json')) { html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid var(--border)\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:var(--muted)\">Connected \u00b7 ${ep.route}</div></div>`; continue; }\n      const d = await r.json();\n      const ok = d.success !== false && r.ok;\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid ${ok?'var(--border)':'#fca5a5'}\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:${ok?'var(--muted)':'#dc2626'}\">${ok?'Available':'Service unavailable'}</div>${ok && d.score !== undefined ? '<div style=\"font-size:24px;font-weight:700;margin-top:8px\">'+d.score+'</div>' : ''}</div>`;\n    } catch(e) {\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid #fca5a5\"><div style=\"font-weight:600;font-size:14px\">${ep.label}</div><div style=\"font-size:12px;color:#dc2626\">${e.message}</div></div>`;\n    }\n  }\n  html += '</div>';\n  el.innerHTML = html;\n}\n\nasync function loadPortal_billing_center() {\n  if (!CLIENT) return;\n  const el = document.getElementById('billing_center-body');\n  const endpoints = [\n    { route: '/api/invoice-generate', label: 'View invoices' }\n    { route: '/api/tax-export', label: 'Tax documents' }\n  ];\n  let html = '<div style=\"display:grid;gap:12px\">';\n  for (const ep of endpoints) {\n    try {\n      const url = ep.route + (ep.route.includes('?') ? '&' : '?') + 'email=' + encodeURIComponent(CLIENT.email);\n      const r = await fetch(url);\n      const ct = r.headers.get('content-type') || '';\n      if (!ct.includes('json')) { html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid var(--border)\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:var(--muted)\">Connected \u00b7 ${ep.route}</div></div>`; continue; }\n      const d = await r.json();\n      const ok = d.success !== false && r.ok;\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid ${ok?'var(--border)':'#fca5a5'}\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:${ok?'var(--muted)':'#dc2626'}\">${ok?'Available':'Service unavailable'}</div>${ok && d.score !== undefined ? '<div style=\"font-size:24px;font-weight:700;margin-top:8px\">'+d.score+'</div>' : ''}</div>`;\n    } catch(e) {\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid #fca5a5\"><div style=\"font-weight:600;font-size:14px\">${ep.label}</div><div style=\"font-size:12px;color:#dc2626\">${e.message}</div></div>`;\n    }\n  }\n  html += '</div>';\n  el.innerHTML = html;\n}\n\nasync function loadPortal_partner_referral() {\n  if (!CLIENT) return;\n  const el = document.getElementById('partner_referral-body');\n  const endpoints = [\n    { route: '/api/referrals/share', label: 'Share referral link' }\n    { route: '/api/referral-track', label: 'Track referrals' }\n  ];\n  let html = '<div style=\"display:grid;gap:12px\">';\n  for (const ep of endpoints) {\n    try {\n      const url = ep.route + (ep.route.includes('?') ? '&' : '?') + 'email=' + encodeURIComponent(CLIENT.email);\n      const r = await fetch(url);\n      const ct = r.headers.get('content-type') || '';\n      if (!ct.includes('json')) { html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid var(--border)\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:var(--muted)\">Connected \u00b7 ${ep.route}</div></div>`; continue; }\n      const d = await r.json();\n      const ok = d.success !== false && r.ok;\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid ${ok?'var(--border)':'#fca5a5'}\"><div style=\"font-weight:600;font-size:14px;margin-bottom:4px\">${ep.label}</div><div style=\"font-size:12px;color:${ok?'var(--muted)':'#dc2626'}\">${ok?'Available':'Service unavailable'}</div>${ok && d.score !== undefined ? '<div style=\"font-size:24px;font-weight:700;margin-top:8px\">'+d.score+'</div>' : ''}</div>`;\n    } catch(e) {\n      html += `<div style=\"background:var(--surface);padding:16px;border-radius:12px;border:1px solid #fca5a5\"><div style=\"font-weight:600;font-size:14px\">${ep.label}</div><div style=\"font-size:12px;color:#dc2626\">${e.message}</div></div>`;\n    }\n  }\n  html += '</div>';\n  el.innerHTML = html;\n}",
  "settab_cases": "  if (tab === 'compliance_center') loadPortal_compliance_center();\n  if (tab === 'filing_actions') loadPortal_filing_actions();\n  if (tab === 'entity_mgmt') loadPortal_entity_mgmt();\n  if (tab === 'ai_assistant') loadPortal_ai_assistant();\n  if (tab === 'billing_center') loadPortal_billing_center();\n  if (tab === 'partner_referral') loadPortal_partner_referral();"
}